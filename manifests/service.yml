apiVersion: v1
kind: Service
metadata:
    name: "ecm3440"
    labels:
        app: "ecm3440"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "ecm3440"